// فارسی — Persian.
//
// Empty for now: this release only extracts the English strings into a
// dictionary, leaving the interface byte-identical. Farsi lands next, together
// with the right-to-left layout, at which point 'fa' is added to SUPPORTED in
// js/lang.js.
//
// Any key missing here falls back to English (see i18n.js), so a partial
// dictionary is always safe to ship.

export default {};
